# Diffusion policy on the recorded task2 contract. Second rung: stronger
# than ACT on multimodal demonstrations, slower to train.
policy:
  type: diffusion
  horizon: 32
  n_action_steps: 16
  device: cuda
steps: 200000
batch_size: 8
save_freq: 20000
num_workers: 8
